<section class="section">
  <div class="container">
    <h1 class="title">Shark Traits & Trends</h1>

    <section class="section">
      <h2 class="subtitle">Scientific Name</h2>

      <%= form_tag '' do %>
        <div class="field">
          <%= label_tag 'species', 'Genus & Species', class: 'label' %>
          <div class="control">
            <%= select_tag 'species', nil, style: 'width: 100%', multiple: 'multiple' %>
          </div>
          <p class="help">For example: Carcharhinus acronotus</p>
        </div>

        <div class="field">
          <%= label_tag 'traits', 'Traits', class: 'label' %>
          <div class="control">
            <%= select_tag 'traits', nil, style: 'width: 100%', multiple: 'multiple' %>
          </div>
          <p class="help">For example: Lmat50 See the procedures document for more details <%= link_to 'here', procedure_path(anchor: 'trait-classes-and-traits') %>.</p>
        </div>

        <div class="field is-grouped is-grouped-right">
          <p class="control">
            <%= submit_tag 'Search', class: 'button is-info' %>
          </p>
        </div>
      <% end %>
    </section>

    <section class="section">
      <h2 class="subtitle">Information by Family</h2>

      <%= form_tag '', class: 'field' do %>
        <div class="field is-grouped">
          <p class="control is-expanded">
            <%= select_tag 'family', nil, style: 'width: 100%', multiple: 'multiple' %>
          </p>
          <p class="control">
            <%= submit_tag 'Search', class: 'button is-info' %>
          </p>
        </div>
      <% end %>
    </section>


    <p>The Shark Traits Database is an open source research initiative that aims to
      make all observations and measurements of chondrichthyans (sharks, rays, and
      chimaeras) accessible in order to more rapidly advance resesearch.</p>
  </div>
</section>
